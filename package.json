{
  "name": "crispr-kiosk-player",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone digital-signage / smart-kiosk player runtime. Loads a screen's assigned loop and renders content fullscreen.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  }
}
